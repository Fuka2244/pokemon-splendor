# 选定布局：BGA 桌面

2026-08-21，用户从三套可点击原型中选择了 **A — BGA 桌面布局**。

保留的设计判断：

- 公共精灵球位于左侧。
- 三级野外区域占据页面中央。
- 对手状态在桌面上方横向展示。
- 自己的资源、永久加成、已捕捉与预留宝可梦固定在桌面下方。
- 对局记录位于右侧；卡牌详情以非阻塞侧栏显示。
- 合法操作直接在桌面组件上高亮，上方操作栏只负责确认或取消。

原始 A/B/C 原型保留在 `prototype/`，因为当前目录尚未初始化为 Git 仓库，无法将原型存入独立的 throwaway 分支。
